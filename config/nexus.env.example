# Source before publish (local shell or Jenkins credentials):
#   set -a && source config/nexus.env && set +a

export NEXUS_BASE_URL=http://2.26.86.191:8081

# npm (@example/ui-backend-model)
export NPM_CONFIG_REGISTRY=${NEXUS_BASE_URL}/repository/npm-hosted/
export NEXUS_NPM_TOKEN=replace-me

# maven (com.example:ui-backend-model)
export NEXUS_MAVEN_URL=${NEXUS_BASE_URL}/repository/maven-releases/
export NEXUS_MAVEN_REPOSITORY=maven-releases
export NEXUS_USERNAME=replace-me
export NEXUS_PASSWORD=replace-me
